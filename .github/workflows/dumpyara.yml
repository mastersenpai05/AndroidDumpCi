name: Dump
 
on:
  push:
    paths-ignore:
      - '*.md'
    branches: [ main ]
  workflow_dispatch:

defaults:
  run:
    shell: bash

env:
  ROM_URL: https://index.aospbuilding.workers.dev/0:/Firmware.zip%22&response-content-type=application%2Fzip&X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&X-Amz-Algorithm=AWS4-HMAC-SHA256&X-Amz-Credential=AKIAI75SICYCOZ7DPWTA%2F20211125%2Fap-southeast-1%2Fs3%2Faws4_request&X-Amz-Date=20211125T131858Z&X-Amz-SignedHeaders=host&X-Amz-Expires=1800&X-Amz-Signature=dfbb4a1fb607714b7a458bf10fa20793bc058e3d8812e1eceedcb32cca5c46c1

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
       - name: Checkouts
         uses: actions/checkout@v2

       - name: Clone Dumpyara
         run: |
              git clone https://github.com/AndroidDumpsCI/firmware_dumper

       - name: Setup Dumpyara environment
         working-directory: firmware_dumper
         run: |
              sudo bash setup.sh
              git config --global user.name "mastersenpai05"
              git config --global user.email "pampangavergil0405@gmail.com"

       - name: Setup config envs
         working-directory: firmware_dumper
         run: |
              echo "${{ secrets.GIT_TOKEN }}" > .github_token
              echo "${{ secrets.GIT_ORG_NAME }}" > .github_orgname
              echo "${{ secrets.BOT_TOKEN }}" > .tg_token
              echo "${{ secrets.CHAT_ID }}" > .tg_chat

       - name: Running Dumpyara
         working-directory: firmware_dumper
         run: |
              ./dump.sh "$ROM_URL"
